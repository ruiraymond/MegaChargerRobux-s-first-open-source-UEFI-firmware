# MegaChargerRobux-s-first-open-source-UEFI-firmware

This repository now also includes a ready-to-copy starter template for **TheChilledLemonium-browser** with:
- A project prompt tailored for cross-platform Electron development.
- Installer build configuration for:
  - **Windows `.exe`** (NSIS)
  - **macOS `.dmg`**
  - **Linux `.AppImage`**
- Basic Electron browser shell code and CI workflow for multi-OS artifact generation.

See `templates/TheChilledLemonium-browser/`.
